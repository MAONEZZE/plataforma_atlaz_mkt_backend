[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "atlaz-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy[asyncio]>=2.0",
    "alembic>=1.13",
    "asyncpg>=0.29",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "python-jose[cryptography]>=3.3",
    "supabase>=2.5",
    "httpx>=0.27",
    "structlog>=24.2",
    "sentry-sdk[fastapi]>=2.6",
    "slowapi>=0.1.9",
    "typer>=0.12",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "testcontainers[postgres]>=4.7",
    "ruff>=0.5",
    "black>=24.4",
    "mypy>=1.10",
    "import-linter>=2.1",
    "types-python-jose>=3.3",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*", "scripts*"]

[tool.ruff]
target-version = "py312"
line-length = 99

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "TCH"]
ignore = ["E501", "B008"]

[tool.black]
line-length = 99
target-version = ["py312"]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
exclude = ["alembic/", "scripts/"]

[[tool.mypy.overrides]]
module = ["jose.*", "supabase.*", "slowapi.*", "testcontainers.*", "sentry_sdk.*"]
ignore_missing_imports = true

[tool.importlinter]
root_packages = ["app"]
include_external_packages = true

[[tool.importlinter.contracts]]
name = "Domain does not import infrastructure"
type = "forbidden"
source_modules = [
    "app.contexts.auth.domain",
    "app.contexts.usuarios.domain",
    "app.contexts.metricas.domain",
    "app.contexts.conteudo.domain",
    "app.contexts.comunidade.domain",
]
forbidden_modules = [
    "app.contexts.auth.infrastructure",
    "app.contexts.usuarios.infrastructure",
    "app.contexts.metricas.infrastructure",
    "app.contexts.conteudo.infrastructure",
    "app.contexts.comunidade.infrastructure",
]

[[tool.importlinter.contracts]]
name = "Domain does not import presentation"
type = "forbidden"
source_modules = [
    "app.contexts.auth.domain",
    "app.contexts.usuarios.domain",
    "app.contexts.metricas.domain",
    "app.contexts.conteudo.domain",
    "app.contexts.comunidade.domain",
]
forbidden_modules = [
    "app.contexts.auth.presentation",
    "app.contexts.usuarios.presentation",
    "app.contexts.metricas.presentation",
    "app.contexts.conteudo.presentation",
    "app.contexts.comunidade.presentation",
]

[[tool.importlinter.contracts]]
name = "Domain does not import frameworks"
type = "forbidden"
source_modules = [
    "app.contexts.auth.domain",
    "app.contexts.usuarios.domain",
    "app.contexts.metricas.domain",
    "app.contexts.conteudo.domain",
    "app.contexts.comunidade.domain",
]
forbidden_modules = [
    "fastapi",
    "sqlalchemy",
    "supabase",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "--cov=app --cov-report=term-missing --cov-fail-under=0"
